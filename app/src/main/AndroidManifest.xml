<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pawan.employeeapi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UpdateDeleteActivity"
            android:label="Update or Delete Employee"></activity>
        <activity
            android:name=".SearchActivity"
            android:label="Search Employee" />
        <activity
            android:name=".RegisterEmployeeActivity"
            android:label="Register Employee" />
        <activity
            android:name=".ShowActivity"
            android:label="Show All Employee" />
        <activity
            android:name=".MainActivity"
            android:label="Employee API">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>